tags:
    -   "Producto"
summary: "Elimina un producto existente"
description: "Elimina un producto existente indicando el ID"
operationId: "deleteProductoId"
parameters:
    -   in: "path"
        name: "id"
        description: "ID del Producto"
        required: true

security:
  -   Bearer: []
responses:
    200:
        description: Producto eliminada exitosamente
